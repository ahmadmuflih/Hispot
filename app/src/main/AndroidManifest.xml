<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.edutech.baso.mapsproject">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="id.edutech.baso.mapsproject.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:name="id.edutech.baso.mapsproject.Authentication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="id.edutech.baso.mapsproject.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name="id.edutech.baso.mapsproject.LocationActivity" />
        <activity android:name="id.edutech.baso.mapsproject.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="id.edutech.baso.mapsproject.HomeActivity"
            android:label="@string/title_activity_trending"
            android:parentActivityName="id.edutech.baso.mapsproject.LoginActivity"
            android:theme="@style/MyMaterialTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="id.edutech.baso.mapsproject.LoginActivity" />
        </activity>
        <activity
            android:name="id.edutech.baso.mapsproject.ProfileActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="id.edutech.baso.mapsproject.UserActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="id.edutech.baso.mapsproject.PhotoActivity"
            android:label="Photo"
            android:theme="@style/AppTheme" />
        <activity
            android:name="id.edutech.baso.mapsproject.SubscribersActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="id.edutech.baso.mapsproject.SubscribingActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="id.edutech.baso.mapsproject.SearchActivity"
            android:launchMode="singleTop"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name="id.edutech.baso.mapsproject.FilterActivity"
            android:theme="@style/AppTheme" />
        <activity android:name="id.edutech.baso.mapsproject.PostActivity"
            android:theme="@style/AppTheme" ></activity>
        <activity android:name="id.edutech.baso.mapsproject.SimpleVrPanoramaActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <category android:name="com.google.intent.category.CARDBOARD" />
            </intent-filter>
        </activity>
    </application>

</manifest>